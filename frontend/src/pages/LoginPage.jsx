// 로그인 / 회원가입
export default function LoginPage() {
  // TODO: 로그인·회원가입 폼(api.post('/auth/login'), api.post('/auth/signup')) 구현
  return (
    <section>
      <h1>로그인</h1>
      <p>이메일 · 비밀번호 로그인 / 회원가입</p>
    </section>
  )
}
